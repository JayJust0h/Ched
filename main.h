#ifndef MAIN_H
#define MAIN_H

#include <unistd.h>
#include <stdarg.h>
#include <stdlib.h>

int _printf(const char *format, ...);
int putcher(char c);
int puuts(char *c);
int printf(const char *format, ...);
#endif
